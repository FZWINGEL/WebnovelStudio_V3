# Paste-ready ChatGPT Pro prompt: V3 chat-first author experience

Copy the prompt below into ChatGPT Pro. The product baseline is pinned to the exact commit named below because the repository default branch is older. This is a planning request only: inspect and reason about the current V3 implementation, then write the two requested design documents. Do not implement code in this pass.

```text
You are the lead product/UX architect for WebnovelStudio V3. Inspect https://github.com/FZWINGEL/WebnovelStudio_V3 at exact commit `6ab150505802b6c240db884a83f0a4eb9511df79` on branch `codex/v3-persistence`. The repository default branch is older; do not inspect or cite it as the product baseline. The author's local checkout is `D:\WebnovelStudio_V3`, but do not assume that path is accessible in ChatGPT. If repository access is unavailable, ask for an archive of this commit and clearly distinguish provisional product advice from a source-grounded assessment. Produce a code-grounded UX specification and a bounded implementation plan for a new primary experience: each project is primarily a persistent author-to-AI conversation, with documents and writing surfaces beside it.

The author should be able to begin with a rough idea, an existing note, a scene, a character, a world question, or an ordinary request. They talk naturally. The assistant asks only the few useful questions needed at that moment, infers sensible proposed defaults, and can draft or maintain worldbuilding, characters, plot/themes, plans, notes, chapter material, and related documents. The author reviews those document changes and returns to the conversation to ask for revisions. The order stays flexible: manual writing, direct document editing, and conversation are all valid entry points. English is the product language and export language; optional wuxia/xianxia or translated-webnovel register and terminology may be supported without requiring Chinese authoring.

The assistant should take the initiative in organizing and filling those documents. The author should not need to know which lens, document type, operation, or template to select before asking for help. “Necessary documents” means whatever supports this particular story, not a compulsory world/character/outline checklist. Show the simplest everyday layout: project conversation, relevant documents, and the current chapter when needed. The existing Workshop UI is a migration source, not a mandatory navigation pattern. Preserve valuable backend invariants while allowing old UI workflow rules to change.

Your job is to understand the existing application before proposing changes. Read `AGENTS.md`, `PRODUCT.md`, `docs/README.md`, `docs/IMPLEMENTATION_STATUS.md`, `docs/V3_ARCHITECTURE_REFINED.md`, `docs/V3_FIRST_SLICE_PLAN.md`, `docs/V3_STORY_CONTEXT_SYSTEM.md`, `docs/V3_STORY_CONTEXT_FIRST_SLICE.md`, the existing Workshop UX/specification and implementation documents, and the applicable ADRs. Inspect the actual source and tests, especially the Rust core/library/project/discussion/memory/context/provider modules, Tauri commands and provider runtime, React/Tiptap shell and assistant components, SQLite migrations, and integration/frontend/native checks. Use the baseline commit for citations or file/line references when possible. Treat plans, unchecked boxes, generated reports, and old prose as claims to verify, not as proof. Do not invent capabilities because a document says they are planned. Do not include a private author project title, seed, or author data in the outputs.

Preserve the V3 foundation: native Rust/Tauri plus React/Tiptap, SQLite and versioned persistence, existing projects and documents, editor save boundaries, history and recovery, explicit Apply/adoption and scope checks, frozen source/POV permissions, context and memory authority, multi-project isolation, cancellation and uncertain-operation recovery, and the current direct/manual writing path. Existing backend infrastructure should remain useful. Critically evaluate whether the chat-first experience can be built with existing application concepts, whether a skills-only layer is insufficient, and which additions belong in the app. Do not propose restarting the product, replacing the architecture wholesale, or building a second orchestration framework. Challenge the direction where evidence requires it, but keep the core and describe the smallest coherent evolution.

Provider and runtime constraints are fixed inputs. Codex is the primary author provider. An installed, unpinned Codex CLI may be used through optional persistent stdio/app-server infrastructure with fresh upstream threads; Exec remains the default transport and bounded story lookup remains on Exec. Do not confuse reusing a warm provider process or runtime connection with giving the model raw persistent chat history or authority over story state. The new UX may present one coherent persistent application conversation while each provider request compiles only the allowed, versioned history and current context. Native story tools and history resume are not implemented yet; do not assume they exist. Compare application-controlled structured responses with narrowly scoped story tools if that choice materially affects the design. Any new tool/history capability needs a justified contract, bounded usage, and qualification rather than a silent expansion of provider authority. Keep Claude and OpenAI-compatible HTTP adapters. The author-facing model, reasoning-effort, and Fast/service-tier picker remains independent; maintenance uses GPT-6 Astra/low, with Codex priority and HTTP no tier. Do not silently substitute provider, model, traits, or transport.

Treat transport as an implementation constraint and reuse opportunity, not the center of this exercise. Do not turn the UX documents into an app-server migration plan or a rework of the recently qualified provider plumbing; identify only the transport changes the chat-first contract actually requires, and leave broader parity or migration work explicitly deferred.

Define the trust and persistence model precisely. Separate at least these states and labels: assistant proposal, author-adopted material, draft prose, reviewed evidence, author intent, canonical/source document, rejected or superseded candidate, and private author-room material. A chat message is not canon. AI output must remain a low-friction versioned document draft or reviewable proposal until the author explicitly adopts it; never silently accept every AI edit. Explain when a request may create a draft revision, when it creates a multi-document proposal, and when Apply/adoption is required. For multi-document changes, show a complete preview, affected documents, exact source revisions/heads, scope, protected content, and atomic revision-checked commit behavior. A stale proposal, source change, conflicting author edit, or uncertain acknowledgment must be blocked, retained for inspection, or explicitly rebased; never partially applied or silently retried. Preserve author typing during generation and make older responses visibly stale rather than overwriting newer work.

Make this feel like collaborating with an assistant, not approving every field in a form. Within an author-requested task, AI-created draft material may be saved and organized automatically while remaining separate from accepted material. Propose grouped review and a clear “what changed” view. Evaluate whether an explicit conversational approval can safely reference an exact preview/version through the existing adoption contract; do not make vague assent authorize unspecified edits. Let the author browse and edit the documents directly, then resume the same conversation with its document references intact.

Cover the boundaries that are easy to lose in a conversational UI: author-room versus restricted chapter context; frozen sources and POV permissions; context/memory authority; excluded or rejected candidates; project isolation; copied/duplicated/recovered projects; private facts and future possibilities; manual author edits; stale proposals; interrupted provider runs; lost start/finish acknowledgments; local-save failure; Stop; app close; reconnect/retry; and source changes while an answer is streaming. Explain which facts can update a working document, which require review, and which may enter chapter-writing context only through existing eligibility/briefing rules. Preserve the rule that provider history cannot override the application context snapshot and that a warm upstream connection does not make its history authoritative.

Preserve chapter-wide feedback in chat and feedback anchored to selected words, sentences, or paragraphs. Show how the author can say “make this confrontation more emotional, but keep the ending,” review the resulting edits, and continue discussing them without losing the selected scope. Project creation, switching, and resuming must also remain obvious, with each project's conversation and drafts kept separate.

Address a real observed workflow trap. In a walkthrough, an author tried to start with an idea after a previous question had been marked “Not now”; the request was silently refused. Also, the status/result explanation was buried beneath a long central scroll. Design clear meanings for “Not now,” “Not relevant,” “Keep mysterious,” and “change my mind,” and ensure a valid new request cannot be blocked by an old deferred question. Put current request state, source scope, draft/adoption state, errors, and next useful action where the author can see them without hunting through the transcript. Avoid a giant questionnaire, exhaustive mandatory forms, an “all fields complete” gate, or an endlessly required chain of follow-up questions. Ask one or a few high-value questions when they materially change the current work; otherwise proceed with proposed assumptions that the author can edit or reject. Rough ideas and immediate writing must work.

Produce these two standalone, downloadable Markdown documents, intended for the repository's `docs/` directory. Do not implement code, commit changes, or publish anything in this pass:

1. `docs/V3_CHAT_FIRST_UX_SPEC.md`: the product/interaction contract. Include goals and non-goals; user mental model; project shell and navigation; a concrete chat-plus-document-panel wireframe (desktop and sensible narrow-window behavior); document statuses and labels; request lifecycle; proposal/adoption/revision semantics; context/scope inspector; private author-room and restricted chapter behavior; model/trait/status controls; manual typing; error, Stop, uncertainty, stale-result, and recovery states; multi-project behavior; accessibility and keyboard expectations; and a complete walkthrough from a rough idea to a maintained world/character/plan and then a chapter request. Include at least one existing-note path, one immediate-write path, one interrupted/uncertain operation, and one source-change/stale-proposal path. Show how the assistant asks a useful question, records an assumption, presents a draft, and waits for explicit adoption. Explain what the author sees after returning to chat from a document review. Include an evidence-based “reuse / change / retire / new” table naming current files/modules/concepts and the reason for each disposition. Mark open questions and assumptions separately from requirements.

2. `docs/V3_CHAT_FIRST_UX_IMPLEMENTATION_PLAN.md`: a phased, bounded plan tied to current symbols, storage, IPC, and UI surfaces. Start with the smallest first vertical slice that proves the loop without requiring a full agent, automatic canon extraction, multi-agent debate, or a large new schema. Identify additive migrations or logical mappings only where needed, and explain how existing projects, Workshop state, discussion runs, provider receipts, document revisions, and history are migrated or left intact without data loss. Include explicit implementation order, dependencies, review checkpoints, failure/recovery behavior, and rollback or compatibility boundaries. For each phase, name source files likely to change, tests to add or update, and acceptance gates proportional to risk. Separate deterministic Rust/core tests, frontend/Tiptap tests, native installed/WebView2 checks, and live-provider qualification. Do not claim any test or browser/native result unless you actually ran it; `.local` reports are private local evidence and are unavailable to GitHub readers, so cite them only as local evidence or leave qualification open. Include deferred work and why it is deferred.

The first vertical slice should be concrete enough to build and evaluate: open/create a project; start from a blank idea or existing note; send a natural request; receive at most a small number of useful clarifying questions or inferred assumptions; stream a response while preserving typing; produce one or more reviewable versioned document drafts; inspect exactly what sources and scope were used; adopt or reject explicitly; return to the persistent application conversation; revise one document or make a second linked change; close and reopen with the conversation projection, drafts, source heads, and adoption evidence intact. Preserve the existing distinction between Working, Reviewed, and restricted writing eligibility; do not add compulsory review of every planning document before an author can write. If atomic multi-document adoption is not yet feasible, state a safe first slice boundary and show the later transaction contract rather than pretending it exists.

Include a compact interaction/state model and API/storage mapping, but do not create an abstract intent-state machine or duplicate the story bible. Documents and their revisions remain authoritative for saved material; conversation records should reference exact project/document/revision identities and preserve provenance. Distinguish application conversation history from provider packet history, and define compaction/summarization, retention, disclosure, restore/copy, and unfinished upstream work. Any summary or memory update must have a visible source, scope, status, and review/adoption rule. A persistent chat is a product projection assembled from durable records, not permission for ambient provider history to leak across projects or from author-room into restricted writing.

Design for user usefulness rather than checkbox completion. Propose a small metrics and formative-evaluation plan: time to a knowingly endorsed decision, author ownership, usefulness on return, corrections needed to preserve intent, question burden, navigation/search burden, accidental adoption rate, stale/uncertain recovery success, and ability to explain what will influence a chapter. Include comparable tasks for rough-idea, world-first, character-first, existing-notes, and write-now authors. Do not use raw output volume, acceptance rate, or model-only quality as the sole success measure. Include accessibility, keyboard, visible status, and interruption observations in the evaluation.

End both documents with separate sections for open questions and assumptions. At minimum, flag unresolved choices about conversation retention/compaction, cross-document atomic adoption timing, what “maintain” means for structured versus prose documents, app-server default timing, provider history disclosure, and how much automatic document organization authors want. Make proposed defaults explicit so implementation can proceed while those questions remain open. Keep the writing concise and actionable: this is a serious specification and plan, not a giant wishlist. Cite repository paths and current evidence, call out contradictions or missing evidence, and preserve the boundary that this pass produces design documents only.
```

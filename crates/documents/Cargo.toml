[package]
name = "wns-documents"
version.workspace = true
edition = "2024"
description = "L1: document model, structural scope validation, structured blocks, editor contract."

[dependencies]
wns-kernel = { path = "../kernel" }
serde = { version = "=1.0.229", features = ["derive"] }
serde_json = "=1.0.151"
unicode-segmentation = "=1.13.3"

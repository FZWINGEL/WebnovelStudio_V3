//! F1 staged V2 import installation.
//!
//! Moved to `wns-transfer` (L6); this is the re-export at the historical path,
//! so `crate::projects::import::{…}` and the library's import workflow are
//! unchanged.

pub use wns_transfer::import::*;

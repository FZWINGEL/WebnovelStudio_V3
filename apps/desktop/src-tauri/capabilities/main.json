{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "The local editor trial only. No filesystem, shell, or network plugin permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Local writing window; normal close completes its save guard before destroying the window. File operations stay behind Rust project commands.",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-destroy"]
}
